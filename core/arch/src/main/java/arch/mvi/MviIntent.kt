package arch.mvi

/**
 * Represents a user or system action (intent) that triggers state changes or side effects.
 * Each intent corresponds to an intention to perform some operation, e.g., button click, data refresh.
 */
interface MviIntent
