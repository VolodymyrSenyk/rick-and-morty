package arch.mvi

/**
 * Represents a one-time navigation event, such as navigating to another screen or popping the back stack.
 * Navigation events are distinct from side effects and are handled separately in the UI layer.
 */
interface MviNavEvent
