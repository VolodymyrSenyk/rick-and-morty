package navigation.compose

/**
 * Marker interface for a navigation destination.
 *
 * Typically implemented by sealed classes or objects that define navigation routes
 * in a type-safe way.
 */
interface Destination
